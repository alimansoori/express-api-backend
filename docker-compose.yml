version: '3.8'

services:
  node-app:
    build: .
    image: node-app
    ports:
      - ${PORT}:8080
    volumes:
      - .:/usr/src/node-app
      - /usr/src/node-app/node_modules
    networks:
      - node-network
    env_file:
      - ./.env

networks:
  node-network:
    driver: bridge